# Інструкція з деплою на продакшн

## ⚠️ Важливо
- **Локально:** SQLite база даних
- **Продакшн:** MySQL база даних
- Всі міграції повинні бути застосовані на продакшені

---

## 📋 Перед деплоєм

### 1. Перевірка локальних змін

```bash
# Перевірте статус git
git status

# Переконайтеся, що всі зміни закомічені
git log -1

# Перевірте, що міграції працюють локально
cd laravel
php artisan migrate:status
```

### 2. Створіть резервну копію продакшн бази даних

**На сервері (SSH):**
```bash
# Підключіться до сервера через SSH
ssh user@your-server.com

# Створіть резервну копію MySQL бази даних
mysqldump -u [DB_USER] -p[DB_PASSWORD] [DB_NAME] > backup_$(date +%Y%m%d_%H%M%S).sql

# Або через phpMyAdmin експортуйте базу даних
```

---

## 🚀 Процес деплою

### Крок 1: Завантаження коду на сервер

**Варіант A: Через Git (рекомендовано)**

```bash
# На сервері
cd /path/to/your/project
git pull origin main

# Або якщо використовуєте іншу гілку
git fetch origin
git checkout main
git pull origin main
```

**Варіант B: Через FTP/SFTP**

```bash
# Локально - створіть архів (виключивши node_modules, vendor, .env)
cd /Users/admin/Desktop/WWW/prostar
tar -czf deploy.tar.gz \
  --exclude='node_modules' \
  --exclude='vendor' \
  --exclude='.env' \
  --exclude='database.sqlite' \
  --exclude='.git' \
  --exclude='storage/logs/*' \
  --exclude='storage/framework/cache/*' \
  --exclude='storage/framework/sessions/*' \
  --exclude='storage/framework/views/*' \
  .

# Завантажте deploy.tar.gz на сервер через FTP/SFTP
# Розпакуйте на сервері:
# tar -xzf deploy.tar.gz
```

### Крок 2: Встановлення залежностей

```bash
# На сервері
cd /path/to/your/project/laravel

# Встановіть Composer залежності (якщо ще не встановлено)
composer install --no-dev --optimize-autoloader

# Або якщо вже встановлено, оновіть
composer update --no-dev --optimize-autoloader
```

### Крок 3: Налаштування середовища

```bash
# На сервері
cd /path/to/your/project/laravel

# Переконайтеся, що .env файл налаштований для MySQL
# Відредагуйте .env файл:
nano .env
```

**Перевірте наступні параметри в `.env`:**

```env
APP_ENV=production
APP_DEBUG=false
APP_URL=https://your-domain.com

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=your_database_name
DB_USERNAME=your_database_user
DB_PASSWORD=your_database_password
```

### Крок 4: Застосування міграцій

```bash
# На сервері
cd /path/to/your/project/laravel

# Перевірте статус міграцій
php artisan migrate:status

# Застосуйте нові міграції
php artisan migrate --force

# Якщо виникнуть помилки, перевірте:
# - Чи правильно налаштована база даних в .env
# - Чи існують всі необхідні таблиці
```

**Список нових міграцій, які потрібно застосувати:**

```
2025_11_25_184412_create_hotels_table.php
2025_11_25_184413_create_rooms_table.php
2025_11_25_184428_add_hotel_id_to_tours_table.php
2025_11_25_190012_add_scheme_to_hotels_table.php
2025_11_25_191501_add_is_hostel_to_rooms_table.php
2025_11_25_192252_create_hotel_scheme_categories_table.php
2025_11_25_192253_create_hotel_scheme_items_table.php
2025_11_25_195519_add_meals_to_hotel_scheme_categories_table.php
2025_11_25_202549_add_quantity_to_rooms_table.php
2025_11_25_214815_add_phone_and_hierarchy_to_hotel_scheme_items_table.php
2025_11_25_224135_add_telegram_to_hotel_scheme_items_table.php
2025_11_25_224448_add_advance_and_balance_to_hotel_scheme_items_table.php
2025_11_25_225126_add_transfer_fields_to_hotel_scheme_items_table.php
2025_11_25_225802_add_info_to_hotel_scheme_items_table.php
2025_11_25_230404_add_transfer_prices_to_tours_table.php
2025_11_25_230849_add_room_prices_to_tours_table.php
2025_11_25_232646_add_margin_to_tours_table.php
2025_12_10_200543_add_transfer_toggles_to_tours_table.php
```

### Крок 5: Очищення та оптимізація

```bash
# На сервері
cd /path/to/your/project/laravel

# Очистка кешу конфігурації
php artisan config:clear
php artisan config:cache

# Очистка кешу маршрутів
php artisan route:clear
php artisan route:cache

# Очистка кешу представлень
php artisan view:clear
php artisan view:cache

# Очистка загального кешу
php artisan cache:clear

# Оптимізація автозавантаження
composer dump-autoload --optimize

# Створення символічного посилання для storage (якщо ще не створено)
php artisan storage:link
```

### Крок 6: Налаштування прав доступу

```bash
# На сервері
cd /path/to/your/project/laravel

# Встановіть правильні права доступу
chmod -R 755 storage
chmod -R 755 bootstrap/cache
chown -R www-data:www-data storage bootstrap/cache
# (замініть www-data на відповідного користувача веб-сервера)
```

---

## ✅ Перевірка після деплою

### 1. Перевірка бази даних

```bash
# На сервері
cd /path/to/your/project/laravel

# Перевірте, що всі міграції застосовані
php artisan migrate:status

# Перевірте підключення до бази даних
php artisan tinker
# В tinker виконайте:
# DB::connection()->getPdo();
# exit
```

### 2. Перевірка веб-інтерфейсу

- Відкрийте адмін-панель: `https://your-domain.com/admin`
- Перевірте, що всі сторінки завантажуються
- Перевірте функціонал:
  - ✅ Створення/редагування готелів
  - ✅ Створення/редагування турів
  - ✅ Схема готелю працює
  - ✅ Калькулятор туру працює

### 3. Перевірка логів

```bash
# На сервері
cd /path/to/your/project/laravel

# Перевірте логи на помилки
tail -f storage/logs/laravel.log

# Або через веб-інтерфейс (якщо є)
```

---

## 🔄 Відкат змін (якщо щось пішло не так)

### Відкат міграцій

```bash
# На сервері
cd /path/to/your/project/laravel

# Відкат останньої міграції
php artisan migrate:rollback --step=1

# Або відкат до конкретного batch
php artisan migrate:rollback --step=[N]

# Відновіть базу даних з резервної копії
mysql -u [DB_USER] -p[DB_PASSWORD] [DB_NAME] < backup_YYYYMMDD_HHMMSS.sql
```

### Відкат коду

```bash
# На сервері
cd /path/to/your/project

# Відкат до попереднього коміту
git log --oneline -5  # знайдіть потрібний коміт
git checkout [COMMIT_HASH]

# Або відкат до попереднього тегу
git checkout [TAG_NAME]
```

---

## 📝 Чеклист деплою

- [ ] Створено резервну копію бази даних
- [ ] Всі зміни закомічені в git
- [ ] Код завантажено на сервер
- [ ] Composer залежності встановлено/оновлено
- [ ] `.env` файл налаштований для MySQL
- [ ] Міграції застосовано (`php artisan migrate`)
- [ ] Кеш очищено та перестворено
- [ ] Права доступу налаштовано
- [ ] Символічне посилання storage створено
- [ ] Адмін-панель відкривається
- [ ] Функціонал готелів працює
- [ ] Функціонал турів працює
- [ ] Схема готелю працює
- [ ] Калькулятор туру працює
- [ ] Логи перевірено на помилки

---

## 🆘 Усунення проблем

### Проблема: Помилка підключення до бази даних

```bash
# Перевірте .env файл
cat .env | grep DB_

# Перевірте підключення вручну
mysql -u [DB_USER] -p[DB_PASSWORD] -h [DB_HOST] [DB_NAME]
```

### Проблема: Міграції не застосовуються

```bash
# Перевірте статус міграцій
php artisan migrate:status

# Спробуйте застосувати міграції з verbose
php artisan migrate --force -v

# Перевірте логи
tail -f storage/logs/laravel.log
```

### Проблема: Помилки 500 на сторінках

```bash
# Очистіть всі кеші
php artisan optimize:clear

# Перевірте права доступу
ls -la storage bootstrap/cache

# Перевірте логи
tail -50 storage/logs/laravel.log
```

### Проблема: Файли не завантажуються

```bash
# Перевірте символічне посилання
ls -la public/storage

# Якщо немає, створіть
php artisan storage:link

# Перевірте права доступу
chmod -R 755 storage/app/public
```

---

## 📞 Контакти та підтримка

Якщо виникли проблеми під час деплою:
1. Перевірте логи: `storage/logs/laravel.log`
2. Перевірте логи веб-сервера (Apache/Nginx)
3. Перевірте помилки PHP в логах сервера

---

**Останнє оновлення:** 2025-11-26  
**Версія:** 1.1 (з схемою готелю та калькулятором туру)


