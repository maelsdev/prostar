# ProStar Travel - Інструкція з деплою

## Версія: 1.2

### Процес деплою: Код → Коміт → Деплой → Тестування

---

## 🚀 Швидкий деплой (автоматичний скрипт)

**Найпростіший спосіб - використовуйте скрипт `deploy.sh`:**

```bash
./deploy.sh "Опис ваших змін"
```

Скрипт автоматично виконає:
1. ✅ Перевірку статусу Git
2. ✅ Коміт змін
3. ✅ Push на GitHub
4. ✅ Деплой на сервер
5. ✅ Очищення та оптимізацію кешів
6. ✅ Перевірку синхронізації

**Приклад:**
```bash
./deploy.sh "Додано новий функціонал бронювання"
```

---

## 📋 Крок 1: Підготовка коду (якщо використовуєте ручний деплой)

1. Переконайтеся, що всі зміни протестовані локально
2. Перевірте чи немає помилок лінтера
3. Очистіть зайві файли та кеші

---

## 📝 Крок 2: Git операції (локально) - для ручного деплою

```bash
# Перевірка статусу
git status

# Додавання змін
git add -A

# Коміт з описом змін
git commit -m "Опис змін"

# Якщо є конфлікти з віддаленим репозиторієм
git pull --rebase origin main

# Push на GitHub
git push origin main
```

---

## 🚀 Крок 3: Деплой на сервер (ручний спосіб)

### Якщо не використовуєте скрипт `deploy.sh`, виконайте вручну:

```bash
# SSH підключення до сервера
sshpass -p 'Dthjybrf777' ssh -o StrictHostKeyChecking=no prostar@prostartravel.com

# На сервері:
cd /home/prostar/public_html
git fetch origin
git reset --hard origin/main

# Оновлення залежностей (якщо потрібно)
cd laravel
composer install --no-dev --optimize-autoloader --no-interaction

# Очищення кешів
php artisan config:clear
php artisan cache:clear
php artisan view:clear
php artisan route:clear

# Оптимізація для production
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

### Виконання міграцій (якщо є нові):

```bash
cd /home/prostar/public_html/laravel
php artisan migrate --force
```

---

## ✅ Крок 4: Тестування

1. Перевірте головну сторінку сайту
2. Перевірте адмін-панель: `/admin`
3. Перевірте функціонал, який був доданий/змінений
4. Перевірте консоль браузера на помилки
5. Перевірте логи: `laravel/storage/logs/laravel.log`

---

## 🔧 Корисні команди

### Очищення кешів на сервері:
```bash
sshpass -p 'Dthjybrf777' ssh -o StrictHostKeyChecking=no prostar@prostartravel.com "cd /home/prostar/public_html/laravel && php artisan config:clear && php artisan cache:clear && php artisan view:clear && php artisan route:clear"
```

### Перевірка версії на сервері:
```bash
sshpass -p 'Dthjybrf777' ssh -o StrictHostKeyChecking=no prostar@prostartravel.com "cd /home/prostar/public_html && git log --oneline -1"
```

### Перевірка синхронізації:
```bash
# Локально
git rev-parse HEAD

# На сервері
sshpass -p 'Dthjybrf777' ssh -o StrictHostKeyChecking=no prostar@prostartravel.com "cd /home/prostar/public_html && git rev-parse HEAD"
```

---

## 📦 Створення тегу версії

```bash
# Після успішного деплою та тестування
git tag -a v1.2 -m "Prostar 1.2 - Опис змін"
git push origin v1.2
```

---

## ⚠️ Важливо

- **Ніколи не комітьте `.env` файли**
- **Завжди тестуйте локально перед деплоєм**
- **Перевіряйте синхронізацію після деплою**
- **Очищайте кеші після змін конфігурації**
- **Виконуйте міграції після оновлення коду**

---

## 🔐 Доступ до сервера

- **Хост:** prostar@prostartravel.com
- **Пароль:** Dthjybrf777
- **Шлях:** /home/prostar/public_html
- **Git репозиторій:** https://github.com/maelsdev/prostar

---

## 📝 Приклад повного деплою

```bash
# 1. Локально - коміт
git add -A
git commit -m "Додано новий функціонал X"
git push origin main

# 2. На сервері - деплой
sshpass -p 'Dthjybrf777' ssh -o StrictHostKeyChecking=no prostar@prostartravel.com "cd /home/prostar/public_html && git fetch origin && git reset --hard origin/main && cd laravel && php artisan config:clear && php artisan cache:clear && php artisan view:clear && php artisan migrate --force && php artisan config:cache && php artisan route:cache && php artisan view:cache"
```

---

---

## 🎯 Рекомендований процес

1. **Розробка** - пишіть код локально
2. **Тестування** - перевіряйте локально
3. **Деплой** - використовуйте `./deploy.sh "Опис змін"`
4. **Перевірка** - тестуйте на сервері

---

**Останнє оновлення:** 2025-12-10  
**Версія документа:** 1.1
